export const API_BASE_URL = window.location.origin;
export const STORAGE_KEYS = Object.freeze({ token: "wayne_access_token" });
export const ROLES = Object.freeze({ ADMIN: "Admin", MANAGER: "Gerente", EMPLOYEE: "Funcionário" });
